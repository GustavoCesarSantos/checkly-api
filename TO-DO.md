1. Verificar se o uso de ponteiros no service: CheckUrl está correto, principalmente na hora de
   instancia e usar no handler: CreateUrl

2. Pensar em como deve ser o fluxo caso a validação da url retorne status code de erro

3. Seguir com o fluxo de implementação do handler: CheateUrl
